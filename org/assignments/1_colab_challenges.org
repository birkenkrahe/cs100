#+STARTUP: overview hideblocks indent entitiespretty: 
* Challenges (home assignment)

1. Print your favorite number:
   #+begin_src python :results output :exports both :session *Python* :python python3
     print(1/137) # the fine-structure constant (Sommerfeld's constant)
   #+end_src

   #+RESULTS:
   : 0.0072992700729927005

2. Create two variables with numbers and print their sum:
   #+begin_src python :results output :exports both :session *Python* :python python3
     x = 33
     y = 12
     print(x + y)
   #+end_src

   #+RESULTS:
   : 45

3. Print the numbers 1 through 5 using a loop
   #+begin_src python :results output :exports both :session *Python* :python python3
     for i in range(5):
         print(i + 1)
   #+end_src

   #+RESULTS:
   : 1
   : 2
   : 3
   : 4
   : 5

4. Generate three random numbers between 1 and 20 using a loop:
   #+begin_src python :results output :exports both :session *Python* :python python3
     import random

     for i in range(3):
         print(random.randint(1,20))
   #+end_src

   #+RESULTS:
   : 8
   : 8
   : 20

5. Make a graph with your own =x= and =x= values:

   #+begin_src python :file ../img/sommerfeldt.png :results file graphics output :exports both :session *Python* :python python3
     import matplotlib.pyplot as plt

     x = ["Pi", "Euler", "Sommerfeld"]
     y = [3.14, 2.71, 1/137]

     plt.plot(x, y, marker="o", linestyle="--")
     plt.title("Pi, Euler number, and Sommerfeld's constant")
     plt.savefig("../img/sommerfeldt.png")
   #+end_src

   #+RESULTS:
   [[file:../img/sommerfeldt.png]]

   These constants are also built-in:
   #+begin_src python :results output :exports both :session *Python* :python python3
     import math
     from scipy.constants import fine_structure

     print(math.pi) # Pi
     print(math.e)  # exp(1) = e
     print(fine_structure)
   #+end_src

   #+RESULTS:
   : 3.141592653589793
   : 2.718281828459045
   : 0.0072973525643
