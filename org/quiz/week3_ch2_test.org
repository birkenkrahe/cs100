#+TITLE: CS 100 — Week 3 Quiz
#+AUTHOR: Marcus Birkenkrahe, PhD
#+SUBTITLE: Think Python Ch 2 · DataCamp "Introduction to Python" Ch 2
#+STARTUP: overview hideblocks indent entitiespretty
#+OPTIONS: toc:nil num:nil ^:nil

Points per question: 1

** 1. How do you create a list containing 1, 2, and 3 in Python?

- ( ) =(1, 2, 3)=
- (x) =[1, 2, 3]=
- ( ) ={1, 2, 3}=
- ( ) =list(1, 2, 3)=

** 2. Can a Python list contain elements of different types?

- ( ) No, all elements must be the same type
- (x) Yes, a list can contain mixed types
- ( ) Only integers and floats can be mixed
- ( ) Only if you declare the types first

** 3. What is the index of the first element in a Python list?

- ( ) 1
- (x) 0
- ( ) -1
- ( ) It depends on the list

** 4. What does the index =-1= refer to?

- (x) The last element of the list
- ( ) An invalid index
- ( ) The first element
- ( ) The middle element

** 5. In the slice =fam[2:5]=, which indices are included?

- ( ) 2, 3, 4, 5
- (x) 2, 3, 4
- ( ) 3, 4, 5
- ( ) 2 and 5 only

** 6. Which operator combines two lists into one?

- ( ) =*=
- ( ) =&=
- (x) =+=
- ( ) =,=

** 7. Which of the following is a valid Python variable name?

- ( ) =2fast=
- ( ) =my-var=
- (x) =my_var2=
- ( ) =class=

** 8. What does the === sign do in a Python assignment statement?

- ( ) Tests whether two values are equal
- (x) Assigns a value to a variable
- ( ) Declares the variable's type
- ( ) Compares two expressions

** 9. Why can't you use =for= as a variable name in Python?

- ( ) It starts with a lowercase letter
- ( ) It is too short
- (x) It is a reserved keyword
- ( ) It is not a noun

** 10. What must you write before using =math.sqrt()=?

- ( ) =load math=
- ( ) =require math=
- (x) =import math=
- ( ) =use math=

** 11. What symbol begins a comment in Python?

- ( ) double slash
- ( ) double dash
- (x) hash symbol
- ( ) slash-star

** 12. Which error type means Python cannot understand your code at all?

- ( ) Runtime error
- ( ) Semantic error
- (x) Syntax error
- ( ) Logic error

** 13. What does =type(["a", 1, True])= return?

- ( ) =class 'mixed'=
- ( ) =class 'tuple'=
- (x) =class 'list'=
- ( ) =class 'array'=

** 14. Given =x = [10, 20, 30, 40]=, what is =x[-2]=?

- ( ) 10
- ( ) 40
- (x) 30
- ( ) 20

** 15. Given =x = [1, 2, 3, 4, 5]=, what does =x[:3]= return?

- ( ) =[2, 3, 4]=
- ( ) =[1, 2, 3, 4]=
- (x) =[1, 2, 3]=
- ( ) =[3, 4, 5]=

** 16. After =del(x[0])=, what happens to the remaining elements?

- ( ) They keep their original indices
- (x) They each shift down by one index
- ( ) The list is reversed
- ( ) The list is cleared

** 17. After =y = x= (where =x= is a list), changing =y[0]= also changes =x[0]=. Why?

- ( ) Python automatically syncs all list variables
- ( ) Python made a linked copy
- (x) Both variables point to the same list in memory
- ( ) Lists are immutable so nothing actually changes

** 18. Which line correctly assigns 3.14 to a variable named =pi_approx=?

- ( ) =pi_approx == 3.14=
- ( ) =3.14 = pi_approx=
- (x) =pi_approx = 3.14=
- ( ) =var pi_approx = 3.14=

** 19. Which of the following is a statement rather than just an expression?

- ( ) =2 + 2=
- ( ) =len("hello")=
- (x) =x = 2 + 2=
- ( ) =42=

** 20. After =import math=, how do you compute the square root of 9?

- ( ) =sqrt(9)=
- ( ) =math.squareroot(9)=
- (x) =math.sqrt(9)=
- ( ) =math(sqrt(9))=

** 21. Your code runs without crashing but gives the wrong answer. What type of error is this?

- ( ) Syntax error
- ( ) Runtime error
- (x) Semantic error
- ( ) Import error

** 22. Which character is allowed inside a Python variable name?

- ( ) Space
- ( ) Hyphen
- (x) Underscore
- ( ) Exclamation mark

** 23. What does =["a", "b"] + ["c"]= produce?

- ( ) =["a", "b", "c", "c"]=
- (x) =["a", "b", "c"]=
- ( ) ="abc"=
- ( ) =TypeError=

** 24. Given =x = [1, 2, 3]=, after =x[1] = 9=, what is =x=?

- ( ) =[9, 2, 3]=
- ( ) =[1, 2, 9]=
- (x) =[1, 9, 3]=
- ( ) =[1, 2, 3, 9]=

** 25. Which of these creates an independent copy of list =x=?

- ( ) =y = x=
- (x) =y = x[:]=
- ( ) =y = x + 0=
- ( ) =y = copy(x)=

** 26. What is printed?

#+BEGIN_EXAMPLE python
x = 5
x = x + 1
print(x)
#+END_EXAMPLE

- ( ) =5=
- (x) =6=
- ( ) =x + 1=
- ( ) =NameError=

** 27. What does the following print?

#+BEGIN_EXAMPLE python
import math
print(math.pi)
#+END_EXAMPLE

- ( ) =3=
- ( ) =math.pi=
- (x) =3.141592653589793=
- ( ) =NameError=

** 28. A student writes =primt("hello")=. What type of error occurs?

- ( ) Syntax error
- ( ) Semantic error
- (x) Runtime error
- ( ) No error

** 29. Given =fam = ["liz", 1.73, "emma", 1.68]=, what does =fam[1:3]= return?

- ( ) =["liz", 1.73, "emma"]=
- ( ) =[1.73, "emma", 1.68]=
- (x) =[1.73, "emma"]=
- ( ) =["emma", 1.68]=

** 30. What does =type([[1, 2], [3, 4]])= return?

- ( ) =class 'nested'=
- ( ) =class 'matrix'=
- (x) =class 'list'=
- ( ) =class 'tuple'=

** 31. What does =10 // 3= return?

- ( ) =3.33=
- (x) =3=
- ( ) =1=
- ( ) =10.0=

** 32. What does =10 % 3= return?

- ( ) =3=
- ( ) =0=
- (x) =1=
- ( ) =3.33=

** 33. What does ="ha" * 3= produce?

- ( ) ="ha ha ha"=
- (x) ="hahaha"=
- ( ) =["ha", "ha", "ha"]=
- ( ) =TypeError=

** 34. What does =int(3.9)= return?

- (x) =3=
- ( ) =4=
- ( ) =3.9=
- ( ) =TypeError=

** 35. Given =x = [10, 20, 30, 40, 50]=, what does =x[3:]= return?

- ( ) =[10, 20, 30]=
- ( ) =[20, 30, 40]=
- (x) =[40, 50]=
- ( ) =[30, 40, 50]=

** 36. What does =str(42)= return?

- ( ) =42=
- ( ) =42.0=
- (x) ="42"=
- ( ) =int=

** 37. Given =fam2 = [["liz", 1.73], ["emma", 1.68]]=, what does =fam2[1][0]= return?

- ( ) =1.73=
- ( ) ="liz"=
- (x) ="emma"=
- ( ) =1.68=

** 38. Which also creates an independent copy of list =x=?

- ( ) =y = x=
- ( ) =y = x[0]=
- (x) =y = list(x)=
- ( ) =y = copy(x)=

** 39. What is the result of =2 + 3 * 4= in Python?

- ( ) =20=
- (x) =14=
- ( ) =24=
- ( ) =9=

** 40. If =x = 5=, what does =x += 1= do?

- ( ) Compares =x= to =6=
- ( ) Sets =x= to =1=
- (x) Sets =x= to =6=
- ( ) Raises a =SyntaxError=
